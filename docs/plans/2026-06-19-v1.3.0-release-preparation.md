# v1.3.0 Release Preparation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Integrate the frosty UI cleanup safely, close the remaining release blockers, and prepare SubTrkr v1.3.0 for an explicit tag-and-publish confirmation.

**Current status:** Release preparation is merged and pushed to `main`. v1.3.0 remains unpublished pending explicit tag confirmation.

**Architecture:** Preserve existing WIP as small commits, keep all release-preparation work on `claude/frosty-golick-1f2caa`, then advance `main` only after verification. Styling fixes remain token-driven; dependency remediation stays within compatible versions; release publication follows the repository confirmation gates.

**Tech Stack:** React 19, TypeScript 5.8, Tailwind CSS 4, Bun, Tauri 2, Rust, GitHub Actions.

## Global Constraints

- Use `bun`, never npm, yarn, or pnpm.
- Do not run Supabase migration or database-push commands from this repository.
- Preserve existing user work in both worktrees.
- Run the full TypeScript, Bun test, production build, Rust, audit, and desktop smoke gates before release.
- Stop for explicit review after drafting the changelog and before pushing the release tag.

---

### Task 1: Preserve existing mutation-refresh work

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `reloadItems(): Promise<ItemWithCategory[]>` from `useAppDataSync`
- Produces: immediate item-state refresh after create, update, and lifecycle mutations

- [x] **Step 1: Inspect the existing main-worktree diff**
- [x] **Step 2: Run `bunx tsc --noEmit` and `bun test`**
- [x] **Step 3: Commit the preserved change as `fix(app): refresh items after mutations`**
- [x] **Step 4: Merge current `main` into the frosty worktree**

### Task 2: Fix status-action contrast

**Files:**
- Modify: `src/components/StatusChangeDialog.tsx`
- Test: `src/components/statusActionStyles.test.ts`
- Create: `src/components/statusActionStyles.ts`

**Interfaces:**
- Produces: `getStatusActionButtonStyle(action)` returning token-driven background, foreground, and border values
- Consumes: `StatusChangeData['action']`

- [x] **Step 1: Write tests requiring readable dark foregrounds on bright action fills and neutral archive styling**
- [x] **Step 2: Run the targeted test and confirm it fails because the helper does not exist**
- [x] **Step 3: Implement the minimal style helper and use it in `StatusChangeDialog`**
- [x] **Step 4: Run the targeted test and full test suite**
- [x] **Step 5: Commit the contrast fix**

### Task 3: Remediate dependency audit findings

**Files:**
- Modify: `bun.lock`
- Modify only if required by compatible resolution: `package.json`

**Interfaces:**
- Produces: dependency graph with no known production audit findings

- [x] **Step 1: Update the vulnerable compatible dependency set without major-version upgrades**
- [x] **Step 2: Run `bun audit --production` and `bun audit`**
- [x] **Step 3: Run TypeScript, tests, and production build**
- [x] **Step 4: Commit the dependency remediation**

### Task 4: Close documentation and worktree hygiene

**Files:**
- Move: `docs/plans/2026-05-06-finish-ui-slop-cleanup.md` to `docs/completed-plans/2026-05-06-finish-ui-slop-cleanup.md`
- Modify: `docs/ROADMAP.md`
- Modify: `docs/TASKS.md`
- Modify: `.gitignore`

**Interfaces:**
- Produces: no completed plan in the active queue and no untracked local Codex config

- [x] **Step 1: Preserve the locally deleted UI plan as completed documentation**
- [x] **Step 2: Mark the UI cleanup completed and remove stale hardening priority text**
- [x] **Step 3: Ignore repository-local `.codex/` configuration**
- [x] **Step 4: Verify clean documentation links and commit**

### Task 5: Verify the release candidate

**Files:**
- Verify only

**Interfaces:**
- Produces: recorded evidence for every release gate

- [x] **Step 1: Run `bun install --frozen-lockfile`**
- [x] **Step 2: Run `bunx tsc --noEmit` and `bun test`**
- [x] **Step 3: Run `bun run build` and `cargo check`**
- [x] **Step 4: Run authenticated Tauri smoke checks for item CRUD, lifecycle, analytics, notifications, auth, and updater UI**
- [x] **Step 5: Confirm both worktrees contain no unresolved changes**

### Task 6: Integrate and prepare v1.3.0

**Files:**
- Modify: `package.json`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/tauri.conf.json`
- Modify: `CHANGELOG.md`

**Interfaces:**
- Produces: clean `main` release commit ready for `v1.3.0`

- [x] **Step 1: Advance `main` to the verified frosty branch**
- [x] **Step 2: Bump all repository version declarations to `1.3.0`**
- [x] **Step 3: Draft and prepend the v1.3.0 changelog from commits since v1.2.2**
- [x] **Step 4: Pause for user changelog review**
- [x] **Step 5: After approval, commit and push the release preparation**
- [ ] **Step 6: Pause for explicit tag confirmation**
- [ ] **Step 7: Tag, push, monitor CI, and verify updater artifacts**
